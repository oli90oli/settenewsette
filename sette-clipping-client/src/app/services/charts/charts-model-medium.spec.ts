import { ChartsModelMedium } from './charts-model-medium';

describe('ChartsModelMedium', () => {
  it('should create an instance', () => {
    expect(new ChartsModelMedium()).toBeTruthy();
  });
});
