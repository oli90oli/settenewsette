import { ChartsModelClipsByDate } from './charts-model-clips-by-date';

describe('ChartsModelClipsByDate', () => {
  it('should create an instance', () => {
    expect(new ChartsModelClipsByDate()).toBeTruthy();
  });
});
